sed -i 's/val request = com.example.data.SyncRequest(transactions = transactions)/val sharedPrefs = getApplication<Application>().getSharedPreferences("sfa_prefs", Context.MODE_PRIVATE)\n                        var deviceId = sharedPrefs.getString("device_id", null)\n                        if (deviceId == null) {\n                            deviceId = UUID.randomUUID().toString()\n                            sharedPrefs.edit().putString("device_id", deviceId).apply()\n                        }\n                        val currentSalesId = _loggedInUser.value?.id\n                        val appVersion = "1.0.0"\n                        val request = com.example.data.SyncRequest(\n                            transactions = transactions,\n                            deviceId = deviceId,\n                            appVersion = appVersion,\n                            salesId = currentSalesId\n                        )/g' app/src/main/java/com/example/viewmodel/SfaViewModel.kt
